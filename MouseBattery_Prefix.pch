//
// Prefix header for all source files of the 'MouseBattery' target in the 'MouseBattery' project
//

#ifdef __OBJC__
    #import <Cocoa/Cocoa.h>
#endif

#ifdef DEBUG_MODE
	#define DLog(...) NSLog(@"%s %@", __PRETTY_FUNCTION__, [NSString stringWithFormat:__VA_ARGS__])
#else
	#define DLog(...) do { } while (0)
#endif